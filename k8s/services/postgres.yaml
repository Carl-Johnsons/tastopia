apiVersion: v1
kind: Service
metadata:
  name: postgres
spec:
  type: LoadBalancer
  selector:
    app: postgres
  ports:
  - name: "5432"
    port: 5432
    targetPort: 5432
    nodePort: 30432
    protocol: TCP
