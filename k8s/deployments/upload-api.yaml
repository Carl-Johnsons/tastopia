apiVersion: apps/v1
kind: Deployment
metadata:
  name: upload-api
spec:
  selector:
    matchLabels:
      app: upload-api
  template:
    metadata:
      labels:
        app: upload-api
    spec:
      restartPolicy: Always
      volumes:
        - name: tls-certs
          secret:
            secretName: upload-api-tls
            items:
              - key: tls.crt
                path: tls.crt
              - key: tls.key
                path: tls.key
      containers:
      - name: upload-api
        image: taiduc113/tastopia:upload-api
        volumeMounts:
          - name: tls-certs
            mountPath: /etc/ssl/certs/server-cert.crt
            subPath: tls.crt
          - name: tls-certs
            mountPath: /etc/ssl/private/private-key.pem
            subPath: tls.key
        envFrom:
          - secretRef:
              name: global-secret
          - secretRef:
              name: upload-api-secret
        resources:
          requests:
            cpu: "250m"
            memory: "64Mi"
          limits:
            memory: "500M"
            cpu: "500m"
        ports:
        - containerPort: 80
          protocol: TCP
        - containerPort: 443
          protocol: TCP
