apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-api
spec:
  selector:
    matchLabels:
      app: notification-api
  template:
    metadata:
      labels:
        app: notification-api
    spec:
      restartPolicy: Always
      initContainers:
        - name: db-wait
          image: mongo
          envFrom: 
            - secretRef:
                name: global-secret
          command: 
            - sh
            - -c
            - |
              until mongosh -u $MONGO_INITDB_ROOT_USERNAME -p \
                $MONGO_INITDB_ROOT_PASSWORD --authenticationDatabase admin \
                --quiet --eval "1"; echo $? \
                2>/dev/null; do

                  echo 'Waiting for database to be creatable...'
                  sleep 2
              done

              echo 'Database is online'
        - name: db-migrate
          envFrom: 
            - secretRef:
                name: global-secret
            - secretRef:
                name: notification-api-secret
          image: taiduc113/tastopia:notification-api
          command: ["dotnet","NotificationService.API.dll", "--seed"]
      containers:
      - name: notification-api
        image: taiduc113/tastopia:notification-api
        envFrom:
          - secretRef:
              name: global-secret
          - secretRef:
              name: notification-api-secret
        resources:
          requests:
            cpu: "250m"
            memory: "64Mi"
          limits:
            memory: "500M"
            cpu: "500m"
        ports:
        - containerPort: 80
          protocol: TCP
        - containerPort: 443
          protocol: TCP
