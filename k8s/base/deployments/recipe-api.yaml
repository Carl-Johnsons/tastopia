apiVersion: apps/v1
kind: Deployment
metadata:
  name: recipe-api
spec:
  selector:
    matchLabels:
      app: recipe-api
  template:
    metadata:
      labels:
        app: recipe-api
    spec:
      restartPolicy: Always
      initContainers:
        - name: db-wait
          image: mongo
          envFrom: 
            - secretRef:
                name: global-secret
          command: 
            - sh
            - -c
            - |
              until mongosh -u $MONGO_INITDB_ROOT_USERNAME -p \
                $MONGO_INITDB_ROOT_PASSWORD --authenticationDatabase admin \
                --quiet --eval "1"; echo $? \
                2>/dev/null; do

                  echo 'Waiting for database to be creatable...'
                  sleep 2
              done

              echo 'Database is online'
        - name: db-migrate
          envFrom: 
            - secretRef:
                name: global-secret
            - secretRef:
                name: recipe-api-secret
          image: taiduc113/tastopia:recipe-api
          command: ["dotnet","RecipeService.API.dll", "--seed"]
      containers:
      - name: recipe-api
        image: taiduc113/tastopia:recipe-api
        imagePullPolicy: Always
        envFrom:
          - secretRef:
              name: global-secret
          - secretRef:
              name: recipe-api-secret
        resources:
          requests:
            cpu: "20m"
            memory: "64Mi"
          limits:
            memory: "500M"
            cpu: "500m"
        ports:
        - containerPort: 80
          protocol: TCP
        - containerPort: 443
          protocol: TCP
