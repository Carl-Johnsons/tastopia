---
- name: Init collections
  hosts: localhost
  tasks:
    - name: Run install command
      ansible.builtin.command:
        argv:
          - ansible-galaxy
          - collection
          - install
          - -r
          - requirements.yml
      register: out
      changed_when: out.rc == 0
