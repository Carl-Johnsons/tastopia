namespace Contract.Constants;

public static class COMMAND_ARGS
{
    public const string MIGRATE = "--migrate";
    public const string SEED = "--seed";
    public static readonly string[] All = [MIGRATE, SEED];
}
