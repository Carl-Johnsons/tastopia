apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: website
spec:
  ingressClassName: nginx
  rules:
    - http:
        paths:
          - path: "/tastopia/ci/deploy/dashboard"
            pathType: Prefix
            backend:
              service:
                name: website
                port:
                  number: 80
